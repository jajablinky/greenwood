/**
 * When true: Arweave wallet connect uses the real provider; Ouroboros HTTP,
 * workspace creation, and channel messaging are not called — flows use local mock data.
 */
export const APP_MOCK_ONLY = true
